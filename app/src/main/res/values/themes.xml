<resources>
    <!-- Compose draws all UI; this is only the pre-Compose window background/splash theme. -->
    <style name="Theme.AtmaSanyam" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
