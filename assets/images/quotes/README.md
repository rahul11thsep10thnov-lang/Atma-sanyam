Quote tiles are generated programmatically at runtime (see `src/components/PuzzleContent.tsx`
and `src/data/quotes.ts`) — no image files are needed here for the quote pack itself.

This folder is kept so a future revision can drop in optional background textures/photos
to render behind quote text (referenced from `src/data/quotes.ts`).
